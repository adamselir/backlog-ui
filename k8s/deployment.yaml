apiVersion: apps/v1
kind: Deployment
metadata:
  name: backlog-ui
  namespace: homelab-platform
  labels:
    app.kubernetes.io/name: backlog-ui
    app.kubernetes.io/part-of: homelab-platform
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: backlog-ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: backlog-ui
        app.kubernetes.io/part-of: homelab-platform
    spec:
      # cf-access-proxy is a private GHCR package — the sidecar needs creds to
      # pull it (the backlog-ui app image is public, so the pod otherwise had no
      # pull secret and the sidecar pull failed with anonymous 401).
      imagePullSecrets:
        - name: ghcr-secret
      automountServiceAccountToken: false
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: backlog-ui
          image: ghcr.io/adamselir/backlog-ui:main
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
          env:
            # Bind to 0.0.0.0 so the kubelet can run HTTP probes against pod IP.
            # NetworkPolicy is the real security boundary — no ingress rule
            # allows port 8000 from outside this pod (sidecar connects via
            # loopback 127.0.0.1:8000 at app layer).
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "8000"
            - name: PLATFORM_BASE_URL
              # Public URL — homelab-platform sits behind CF Access, so reaching
              # it requires going through the tunnel + Access app (where the
              # service token policy lives). Cluster-internal Service URL would
              # bypass Access entirely.
              value: https://homelab-platform.amy-eli.com
            - name: REQUEST_TIMEOUT_S
              value: "5"
            - name: LOG_LEVEL
              value: INFO
          envFrom:
            # CF_ACCESS_CLIENT_ID + CF_ACCESS_CLIENT_SECRET — service-token
            # credentials for the `automation-backlog-ui` policy on the
            # homelab-platform Access app. Sealed at k8s/sealedsecret-platform.yaml.
            - secretRef:
                name: backlog-ui-platform
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8000
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 30
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
        - name: cf-access-proxy
          image: ghcr.io/adamselir/cf-access-proxy:0.2.6@sha256:6cde5aace865dac41e79704e22a992b40a97877f08e89d3ae9410a41a372c4a5
          imagePullPolicy: IfNotPresent
          ports:
            - name: https
              containerPort: 3001
          env:
            - name: CF_ACCESS_TEAM_DOMAIN
              value: amy-eli.cloudflareaccess.com
            - name: CF_ACCESS_AUD
              # CF Access app `backlog-ui` (id fe5b8ff5-1317-44bc-833e-67fc10ce5a93)
              value: df8721cc73d6043190118f4239d2cf792d51ca6fdb7214a70e4f2da17b478484
            - name: UPSTREAM_URL
              value: http://127.0.0.1:8000
            - name: LISTEN_PORT
              value: "3001"
            - name: SKIP_PATHS
              value: /healthz,/readyz,/metrics
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            readOnlyRootFilesystem: true
      volumes:
        - name: tmp
          emptyDir: {}
      dnsConfig:
        options:
          - name: ndots
            value: "2"
